services:
  - type: web
    name: debaz-nethra
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run nethra/tn_app.py --server.port $PORT --server.address 0.0.0.0
    plan: free
